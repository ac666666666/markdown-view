# 使用pnpm的严格模式
strict-peer-dependencies=false
auto-install-peers=true

# 设置镜像源（可选，提高下载速度）
registry=https://registry.npmmirror.com/

# 启用shamefully-hoist以提高兼容性
shamefully-hoist=true

# 设置存储目录
store-dir=~/.pnpm-store